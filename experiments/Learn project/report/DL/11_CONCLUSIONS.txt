================================================================================
CONCLUSIONS
Written after the M0 / M2 / M4 / M3 results, plus the Item 4 substitution control and
the Cell 5.8b extended validation.
These experiments were designed to ATTACK the project's own claims. ALL FOUR of
M0/M2/M4/M3 narrowed or corrected one, and the narrowing produced a better paper than
the claims they replaced. Item 4 then UPGRADED a claim (C3) rather than narrowing it.

BOTH REPORTING BUGS IN new_result.txt ARE NOW FIXED AND THE CELLS RE-RUN. This file
carries the corrected values throughout:
  * the M2 `ae` row printed 78.20+-6.24 because it averaged every head width sharing the
    method name. Corrected value 82.00+-0.83 (per-seed 82.15 / 82.93 / 80.91), confirmed
    on re-run.
  * the M3 ensemble row compared `ens-all4`, the PRE-REGISTERED ensemble that FAILED
    (84.88), not the winning perceptual+uncertainty pair. Re-run against the correct
    members it is +1.16 [+0.57, +1.76], p=0.004 — see 1.4, which changes a claim.
================================================================================


################################################################################
# 1. THE NEW EVIDENCE
################################################################################

--------------------------------------------------------------------------------
1.1  M0 — what is the post-hoc variance head actually doing?   (base ae, w=2, 3 seeds)
--------------------------------------------------------------------------------
    full head, per-image log_var            82.00 +- 0.83
    per-image SCALAR only (flat map)        79.87 +- 1.06
    NO HEAD: r^2 / mean-train-residual      69.59 +- 0.82
    STATIC map (train-set pixelwise mean)   69.10 +- 1.03
    plain AE, its own L2 score              68.35 +- 0.65

    variance of log_var explained by the static map : 39.7%
    full - static  = +12.89      full - whiten = +12.41

    SCALAR CONTROL (Cell 5.4, re-run after the labelling fix):
    PRECISION WEIGHT (-log_var) used ALONE as a score   64.42 +- 1.91
    rank corr(log_var, that image's mean residual)          +0.506
    mean log_var    normal -5.1069 | abnormal -5.2066   (abnormal - normal = -0.0997)

    LABELLING CORRECTION, now applied. An earlier version of the code negated log_var in
    BOTH quantities and printed them as log_var's, which flipped the sign of the
    mechanism story. The values above are the corrected ones.

    THE APPARENT TENSION IS RESOLVED, AND THE RESOLUTION IS THE MECHANISM.
    The pooled correlation and the class contrast point in OPPOSITE directions, and that
    is not a contradiction — it is the finding:
      * POOLED  : corr(log_var, residual) = +0.506. Across the population, harder-to-
                  reconstruct images receive a LARGER predicted log_var and are therefore
                  DISCOUNTED. The head has learned expected reconstruction difficulty.
      * BETWEEN : abnormal images receive a SMALLER log_var than normal ones (-0.0997),
                  i.e. exp(-log_var) is 165.2 vs 182.5 — abnormal images are weighted
                  +10.5% HIGHER, receiving LESS discount than their residual alone would
                  predict.
    So the head is NOT tracking residual magnitude. It predicts how hard an image SHOULD
    be, and the anomaly signal is the gap between actual and expected difficulty:

        score = mean( exp(-log_var) * r^2 )  ==  residual RELATIVE TO EXPECTED difficulty
          normal-but-hard : high r^2, HIGH log_var -> ratio stays moderate
          abnormal        : high r^2, LOW  log_var -> ratio is large

    This also explains why -log_var alone is only a weak detector (64.42): the class
    alignment exists but is small (+10.5% in the weight). The discriminative quantity is
    the RATIO, not either factor alone — which is exactly why the divisor is worth +11.52
    while the divisor used as a score is worth only +... nothing over the plain residual.

--------------------------------------------------------------------------------
1.2  M2 — does the head transfer across frozen backbones?          (w=2, 3 seeds)
--------------------------------------------------------------------------------
    backbone   frozen model   + post-hoc head        delta
    ae             68.35      82.00 +- 0.83        +13.65
    ae-ssim        80.81      86.99 +- 0.57         +6.19
    ae-pl          87.58      57.01 +- 2.14        -30.56
    reference: AE-U jointly trained 86.86 +- 0.44

--------------------------------------------------------------------------------
1.3  M4 — capacity or nonlinearity?                                    (3 seeds)
--------------------------------------------------------------------------------
    variant                     params   nonlinear      AUC
    linear   (A4 w=1)              256      no      66.94 +- 1.15
    NONLINEAR                      808      yes     78.27 +- 1.79
    linear                       8,192      no      66.75 +- 1.09
    NONLINEAR (A4 w=2)          14,528      yes     82.00 +- 0.83

--------------------------------------------------------------------------------
1.4  M3 — DeLong PAIRED tests, same 2000 images        (p = least significant seed)
--------------------------------------------------------------------------------
    post-hoc head (w=2) vs plain AE     +13.65  [+12.26, +15.04]   p=5e-75    ***
    AE rescored with SSIM vs AE          +9.08  [ +7.73, +10.42]   p=7.4e-37  ***
    AE-U vs post-hoc head (w=2)          +4.86  [ +3.54,  +6.19]   p=4.3e-09  ***
    ensemble(perc+unc) vs AE-U           +1.88  [ +1.26,  +2.49]   p=1.4e-05  ***
    ensemble(perc+unc) vs AE-PL          +1.16  [ +0.57,  +1.76]   p=0.004    **
    post-hoc head (w=2) vs AE-SSIM       +1.19  [ -0.15,  +2.53]   p=0.66     n.s.

    mean paired 95% half-width 1.10 pts vs the unpaired Hanley-McNeil bound 1.48.

    READ p AND CI AS DIFFERENT AGGREGATIONS — they are not two views of one number.
    The p column is the LEAST SIGNIFICANT SEED (deliberately conservative); the CI is the
    MEAN across seeds. They can disagree sharply. The clearest case is the AE-SSIM row:
    a mean CI of [-0.15, +2.53] implies a pooled p near 0.08, yet the reported p is 0.66.
    So that n.s. verdict is driven by ONE seed, not by the pooled interval. Report both
    aggregations, or pick one and say which.

    THE INSTRUMENT MATTERED. C4's +1.16 sits BELOW the project's own ~1.5-point
    interpretability rule, yet its PAIRED confidence interval excludes zero at p=0.004.
    That rule was derived from an UNPAIRED standard error and must never be applied to
    a comparison of two scores on the same images. Under the correct test C4 is
    significant; under the wrong one it was 'not interpretable'.
    Note the contrast with the post-hoc/AE-SSIM row: a nearly identical point estimate
    (+1.19) is NOT significant, because those two scores are far less correlated. Point
    estimates alone cannot be read off against a fixed threshold.

--------------------------------------------------------------------------------
1.5  ITEM 4 — the C3 substitution control                          (w=2, 3 seeds)
--------------------------------------------------------------------------------
    perceptual + AE-U (jointly trained)          88.74
    perceptual + frozen AE-SSIM + post-hoc head  89.12
    difference                                   +0.38   paired DeLong p = 0.788  n.s.

    Pre-registered rule was |difference| < 1.5 => SUBSTITUTABLE. Satisfied.

    5-MEMBER SCAN — NOW VALIDATED by Cell 5.8b (50 splits x 3 seeds, 31 subsets):
      oracle 89.37 | held-out 89.54 | AE-PL 87.66
      selection bias -0.17 | honest gain +1.88 | beats AE-PL in 100% of splits
      most selected: perceptual+aeu+ssim+head (85% of splits)
      pre-registered 4-member held-out: 88.83  (extended - preregistered = +0.71)
      => SURVIVES. Bias is effectively zero, as for the 4-member set (-0.20), and the
         winner dominates 31 options at 85%. Less unanimous than the 4-member's 100%,
         which is expected over a space twice the size.
      => NOW TESTED (Cell 5.8c). Framed as the single fixed question "does ADDING the
         head member to the pre-registered pair help?", which contains no selection:
             perceptual + uncertainty                88.74
             perceptual + uncertainty + ssim-head    89.46
             difference +0.72, 95% CI [+0.34, +1.10], p=0.0108   SIGNIFICANT
         The third member genuinely adds. 89.46 is the headline; 88.74 is the
         pre-registered pair it improves on.
         CAVEAT to carry: both ensembles share two members, so the interval is tight BY
         CONSTRUCTION — a property of the comparison, not extra evidence. And T5's
         seed caveat (n=3) applies here as everywhere.

    Full-test-set values from the scan (the ranking that was validated above):
      perceptual+aeu+ssim+head       89.46
      perceptual+ssim+head           89.12
      perceptual+aeu                 88.74   <- the pre-registered 4-member winner
      ssim+perceptual+aeu+ssim+head  88.70
      aeu+ssim+head                  88.66


################################################################################
# 2. WHAT IS NOW ESTABLISHED
################################################################################

E1. THE VARIANCE HEAD IS NOT A STATIC ANATOMICAL MASK.            [hypothesis killed]
    The most dangerous alternative explanation was that, with a 16-d latent on
    globally-registered radiographs and a frozen residual as its only target, the head
    would collapse to the pixelwise mean training error — a fixed lung mask. Replacing
    log_var with exactly that map gives 69.10, against the full head's 82.00. The
    per-image component is worth +12.89 and the static component almost nothing.

E2. IT IS NOT REPRODUCIBLE WITH ZERO PARAMETERS.                  [hypothesis killed]
    Dividing the residual by the training-set mean squared residual — no head, no
    training — gives 69.59 against 82.00. Residual whitening does not explain it.

E3. BUT IT IS MOSTLY A PER-IMAGE SCALAR, NOT PER-PIXEL UNCERTAINTY. [major reframing]
    A completely FLAT log_var map, one learned value per image, reaches 79.87. Of the
    head's +13.65 over the plain AE, roughly +11.52 is a per-image scale and only
    ~+2.13 is spatial structure.
    CAUTION on the 39.7%: it is the share of log_var's variance explained by the STATIC
    train-mean map. The remaining 60.3% is everything that map does not explain, which
    includes per-image SPATIAL structure as well as per-image scale — the code's
    decomposition does not separate those two. The evidence for the per-image reading is
    the `scalar` ablation (79.87 of 82.00), NOT the 39.7% figure.
    => The mechanism is a LEARNED PER-IMAGE CALIBRATION of residual magnitude, not
       spatial uncertainty. Images that are globally harder to reconstruct get their
       residual discounted. Calling this "uncertainty" is generous and the paper must
       not lean on the spatial reading.
    => THE FOLLOW-UP CONTROL HAS NOW RUN, and it sharpens E3 into something more
       specific than "a per-image scale":

         PRECISION WEIGHT (-log_var) used ALONE as a score  64.42 +- 1.91
         rank corr(  log_var, that image's mean residual)       +0.506
         rank corr( -log_var, that image's mean residual)       -0.506

       BOTH SIGNS ARE CORRECT AND THEY ARE THE SAME FACT — the second is the negation of
       the first. State the quantity every time. An earlier version of this line read
       "rank corr(scalar, ...) = -0.506" without saying which scalar, which reads as a
       contradiction of the +0.506 in section 1.1 and makes the mechanism look inverted.
       THE MECHANISM QUANTITY IS corr(log_var, residual) = +0.506, un-negated.

       THE SCALAR IS A WEAKER DETECTOR THAN THE RESIDUAL, BUT A USEFUL DIVISOR. The
       precision weight -log_var alone scores 64.42 — above chance, but below the plain
       autoencoder's own 68.35. Dividing the residual by it lifts 68.35 -> 79.87. Its
       value is therefore as a NORMALISER rather than as a score in its own right.
       (Do not write "not a detector": 64.42 is well above chance.)

       REDUNDANCY: settled. Had |rho| been near 1 the score would collapse to a monotone
       transform of the plain L2 score and return 68.35. At 0.506 log_var is related to
       residual magnitude but far from determined by it.

       MECHANISM: NOW ESTABLISHED, and sharper than the original account.
       corr(log_var, residual) = +0.506, so globally harder images are discounted — the
       head has learned EXPECTED reconstruction difficulty. But abnormal images receive a
       SMALLER log_var than normal ones (-0.0997; weight 182.5 vs 165.2, i.e. +10.5%),
       so they are discounted LESS than their residual alone would predict.
       The score is therefore RESIDUAL RELATIVE TO EXPECTED DIFFICULTY. A normal image
       that is merely hard gets a large residual AND a large log_var, and the ratio stays
       moderate; an abnormal image gets a large residual with a SMALL log_var, and the
       ratio is large. That gap is the anomaly signal.
       It also explains the weak standalone AUC: at +10.5% the class alignment in the
       weight is small, so -log_var alone gives only 64.42. The discriminative quantity
       is the ratio, not either factor.

E4. THE "CAPACITY THRESHOLD" WAS NEVER A CAPACITY THRESHOLD.        [claim replaced]
    808 NONLINEAR parameters reach 78.27; 8,192 LINEAR parameters reach 66.75 — 32x the
    capacity, no better than the 256-parameter linear head (66.94). Verified from
    source: BasicBlock(..., last_layer=True) executes `layers = layers[:-2]`, stripping
    the BatchNorm and ReLU, so the original w=1 head was a single ConvTranspose2d with
    no nonlinearity at all, and the w=1 -> w=2 jump confounded width with depth,
    nonlinearity and normalisation.
    => THE CORRECT CLAIM: the variance is NOT LINEARLY DECODABLE from a frozen decoder's
       features. Joint training's function is to make it linearly readable — AE-U's own
       variance branch is a single linear ConvTranspose (256 marginal parameters) and
       reaches 86.86, which is exactly the point.

E5. POST-HOC UNCERTAINTY DOES NOT TRANSFER UNIVERSALLY.               [scope narrowed]
    It lifts the L2-trained backbone (+13.65) and the SSIM-trained one (+6.19), and
    DESTROYS the perceptually-trained one (-30.56, to 57.01). "Retrofit uncertainty onto
    any frozen reconstruction model" is dead. See §3 — the failure is predicted.

E6. AN IMAGENET-FREE ROUTE TO AE-U's ACCURACY.        [new, practical; +0.13 UNTESTED]
    Frozen AE-SSIM + post-hoc head = 86.99 +- 0.57, against jointly-trained AE-U's
    86.86 +- 0.44, using no pre-trained weights and touching no reconstruction weight.
    It also beats AE-SSIM's own native score by +6.19 — a case where a NON-NATIVE score
    beats the diagonal (see §3).

E7. THE IRREDUCIBLE GAP IS REAL, AND THE FREE RESCORE GAIN IS REAL.  [now with statistics]
    AE-U over the post-hoc head: +4.86, p=4.3e-09. Plain AE rescored with SSIM: +9.08,
    p=7.4e-37. Post-hoc over plain AE: +13.65, p=5e-75. All by paired DeLong.

E8. THE POST-HOC HEAD DOES NOT BEAT RETRAINED AE-SSIM.                [claim retracted]
    +1.19, 95% CI [-0.15, +2.53], p=0.66, not significant. The earlier "beats retrained
    AE-SSIM" phrasing is withdrawn; "reaches the level of" is the supported form.

E9. C4's ENSEMBLE GAIN IS STATISTICALLY SIGNIFICANT.            [claim UPGRADED]
    STALE FILES THIS SUPERSEDES, all of which need updating rather than silently
    contradicting: 06 §4.1 "82.00 BEATS fully-retrained AE-SSIM" (retracted, E8/R3);
    06 §4.2 "THE CAPACITY THRESHOLD IS SHARP" (replaced, E4/R1); 07 X3 "AE-U ... cannot
    be replaced by the post-hoc head" (needs a standalone-vs-component qualifier, E10).
    07 T5 is NOT superseded — see below.
    perceptual+uncertainty over AE-PL: +1.16, paired 95% CI [+0.57, +1.76], p=0.004;
    over AE-U: +1.88 [+1.26, +2.49], p=1.4e-05.
    This CONFIRMS A PRE-REGISTERED EXPECTATION rather than reversing a position:
    10_PAPER_FOUNDATION S5 already stated that the correct test is paired DeLong and
    that the unpaired bound must not be applied to it. M3 is that test.

    WHAT IT DOES *NOT* DISPOSE OF — and this correction matters. 07 T5 reads
    "n=3 SEEDS ... inadequate for sub-1.5-point differences": it is a SEED-COUNT
    argument. DeLong treats the 2000 test IMAGES as the sampling unit and contains no
    seed-variance component at all (the "least significant seed" convention in M3 is an
    acknowledgement of exactly that). So the paired test establishes the difference is
    real GIVEN THESE THREE TRAINED MODELS on this test set; it does not establish that
    it survives retraining. BOTH caveats stand, and T5 must NOT be deleted or restated
    as "unpaired only". The unpaired 1.48 bound is a separate quantity introduced later.

    THE CONTRAST WITH E8, stated with its mechanism. E8's +1.19 is LARGER than E9's
    +1.16 yet is not significant. The structural reason is not merely "less correlated":
    the E9 comparison pits an ensemble against a method that is ITS OWN MEMBER (the
    perceptual score is inside perceptual+uncertainty), which is near-maximally paired —
    half-width 0.595 against E8's 1.34.
    An ALTERNATIVE explanation must be acknowledged: E8's p=0.66 is a least-significant-
    SEED p, and the post-hoc head's seed sd is 0.83 against AE-SSIM's 0.35. Seed
    instability is at least as plausible a driver of that verdict as score decorrelation.
    Two explanations, one result — say so.

E10. THE POST-HOC HEAD SUBSTITUTES FOR AE-U INSIDE THE ENSEMBLE.       [C3 upgraded]
    perceptual + jointly-trained AE-U        88.74
    perceptual + frozen AE-SSIM + head       89.12     difference +0.38, p=0.788 n.s.
    Pre-registered as |difference| < 1.5 => substitutable. C3 therefore upgrades from
    "matches AE-U standalone" to "matches AE-U as a component".
    STATE THE SCOPE PRECISELY: this removes the need for uncertainty TRAINING, not for
    ImageNet — the perceptual member still requires VGG-19. And 89.12 may NOT be
    reported as beating 88.74; the difference is not significant. The claim is
    NO DETECTABLE DIFFERENCE, which is weaker than demonstrated equivalence: p=0.788 is
    a failure to reject, and no CI on the +0.38 was computed, so a true difference beyond
    +/-1.5 is not excluded. The pre-registered point-estimate rule is satisfied; a TOST
    against the +/-1.5 margin would be needed to claim equivalence proper.


################################################################################
# 3. THE UNIFYING MECHANISM — and the correction it needs
################################################################################
The working hypothesis was: "a score reads a residual only if it lives in the space the
objective shaped it for." Tested against all the evidence, that is nearly right but
SYMMETRIC, and the data are ASYMMETRIC. The corrected statement is stronger.

THE EVIDENCE, row by row (AUC; `unc` = uncertainty score, from A4/M2):

    trained        l2     grad   perc   ssim   unc      range   below chance
    l2           68.3    73.9   75.9   77.4   82.0      13.7    none
    ssim         61.9    75.2   68.2   80.8   87.0      25.1    none
    perceptual   44.9    64.9   87.6   44.0   57.0      43.6    l2, ssim
    aeu          65.4    71.7   73.6   75.1   86.9      21.5    none

THE PATTERN
    Only ONE row contains catastrophic cells, and they are exactly the PIXEL-SPACE
    scores applied to the PERCEPTUALLY-trained model. Every other pairing is merely
    better or worse, never broken.

THE CORRECTED LAW
    An objective CONSTRAINS a subspace; a score can only read what the objective
    constrained. It is not symmetric:
      * L2 and SSIM constrain pixel intensities — and a pixel-faithful reconstruction is
        also approximately feature-faithful. So pixel-trained models are readable by
        pixel-space AND feature-space scores (AE: 68.3 / 77.4 / 75.9 / 82.0, all usable).
      * The perceptual objective constrains deep features and does NOT constrain
        absolute pixel intensity. The reconstruction is therefore free to drift in pixel
        space, and every pixel-space reading of it collapses (44.9, 44.0, 57.0).
    => pixel-constrained model  -> readable by pixel AND feature scores
       feature-constrained model -> readable by feature scores ONLY

WHAT THIS EXPLAINS, IN ONE MECHANISM
    (a) A1's grid, including why the perceptual row alone falls below chance;
    (b) why AE-U's advantage is scoring rather than reconstruction — its objective
        constrains pixels, so a better pixel-space reading is available to it;
    (c) M2's whole pattern: the uncertainty score is a PIXEL-SPACE score
        (exp(-log_var)*(x-x_hat)^2), so it lifts the two pixel-trained backbones and
        destroys the perceptual one;
    (d) quantitatively, the AE-PL post-hoc collapse (57.01) and A1's perceptual/L2 cell
        (44.9) are the SAME phenomenon — and the uncertainty weighting even partially
        rescues the pixel reading of that model, 44.9 -> 57.01, without coming close to
        its own perceptual score's 87.6.

WHERE THE SIMPLE VERSION OF THE LAW FAILS — state this, do not hide it
    "The diagonal always wins" is FALSE, twice:
      * the L2-trained model is better read by SSIM (77.4) than by L2 (68.3);
      * the SSIM-trained model is better read by the uncertainty score (86.99) than by
        SSIM (80.81).
    Within a compatible space, scores transfer freely and a non-native score can beat
    the native one. The constraint is the SPACE, not the identity of the metric.


################################################################################
# 4. STATUS OF THE PROJECT'S EARLIER CLAIMS
################################################################################
REFUTED
  R1. "A sharp capacity threshold at 0.62% of parameters."  -> nonlinearity, not capacity (E4)
  R2. "Post-hoc uncertainty is a general wrapper."          -> fails on AE-PL (E5)
  R3. "82.00 beats fully retrained AE-SSIM."                -> +1.19, n.s. (E8)
  R4. "The head learns per-pixel uncertainty."              -> mostly a per-image scalar (E3)

NARROWED
  N1. C2 applies to PIXEL-SPACE-trained backbones only.
  N2. "Uncertainty" should be described as a learned per-image residual calibration.
  N3. The recovery fraction must be quoted against both denominators: 74% from the plain
      AE's own L2 score (68.35), 49% from the free SSIM rescore of the same weights (77.4).

CONFIRMED / STRENGTHENED
  S1. A1's specialisation finding — strengthened, and now asymmetric (§3).
  S2. The irreducible gap at w=2 — +4.86, p=4.3e-09 (E7). (4.81 is the w=4
      figure from 06; DeLong tested w=2.)
  S3. The free SSIM rescore gain — significant, p=7.4e-37 (E7).
  S4. C2 survives its two strongest attacks (E1, E2).
  S5. Paired testing is substantially tighter FOR COMPARISONS WITH SHARED/NESTED
      members: the mean half-width is 1.10 vs the unpaired 1.48, but four of the six
      rows sit at 1.32-1.39, barely inside it. The whole advantage lives in the two
      ensemble rows, where one side contains the other.
  S6. C4's ensemble gain is SIGNIFICANT (E9) — a reversal of the earlier position.
  S7. C3 upgraded to component-level substitutability (E10).
  S8. E3 sharpened: -log_var is a WEAKER detector than the residual (64.42 vs 68.35) but
      a useful divisor (68.35 -> 79.87). Mechanism still conditional — see 1.1.

SURVIVED UNCHANGED
  The ten-method reproduction (9/10 within 1 sd); the A1 grid; the A3 ensemble result
  and its pre-registered negative control.


################################################################################
# 5. CONTRIBUTIONS AS THEY SHOULD NOW STAND
################################################################################
Versus 10_PAPER_FOUNDATION r2 the package is NARROWER IN SCOPE and SUBSTANTIALLY
BETTER SUPPORTED. The scope reduction is itself content: C2's failure on AE-PL is the
sharpest confirmation of C1 in the project, because the theory predicted the failure of
a tool we had built for another purpose.

C1  THE ASYMMETRIC SPECIALISATION LAW.                                   [primary]
    Crossing four objectives with five scores, an objective does not improve the
    reconstruction — it constrains a subspace, and a score reads only what was
    constrained. Pixel-constrained models are readable by pixel and feature scores;
    the feature-constrained model is readable by feature scores only, and falls below
    chance under pixel scores (44.9, 44.0).
    EVIDENCE: the grid; interaction terms +9.8 and +35.1 exceeding both main effects;
    the free SSIM rescore +9.08 (p=7.4e-37); and M2's independent confirmation.
    SCOPE: RSNA, image level, 64x64, image-reconstruction methods.

C2  A PROBE THAT TESTS C1 AND OBTAINS A CELL THE GRID CANNOT COMPUTE.    [primary]
    Fitting a variance head to a frozen autoencoder supplies the otherwise-undefined
    plain-AE + uncertainty-score cell, and shows AE-U's advantage is a property of its
    score, not of a better reconstruction (AE-U read with L2 = 65.4, below the plain
    AE's 68.35). It is NOT a static mask (69.10), NOT zero-parameter whitening (69.59),
    is dominated by a per-image scale rather than spatial structure (79.87 of 82.00),
    and requires NONLINEARITY rather than capacity (808 nonlinear 78.27 vs 8,192 linear
    66.75). It transfers to pixel-trained backbones and destroys the perceptual one,
    exactly as C1 predicts.
    SCOPE: a decomposition instrument, NOT a deployment recipe. It costs more wall-clock
    than training AE-U from scratch and lands 4.86 below it (p=4.3e-09).

C3  AN IMAGENET-FREE ROUTE TO THE UNCERTAINTY AUTOENCODER'S ACCURACY.  [supporting]
    Frozen AE-SSIM + post-hoc head = 86.99 +- 0.57 vs jointly-trained AE-U 86.86 +- 0.44,
    with no pre-trained weights and no reconstruction retraining, and +6.19 over
    AE-SSIM's own score. Practically relevant because AE-PL, the strongest single
    method, depends entirely on ImageNet features.

C4  SCORE COMPLEMENTARITY.                                             [supporting]
    perceptual + uncertainty = 88.74 +- 0.13, held-out 88.83, selected in 100% of 150
    split-half selections, reported with its pre-registered negative control (both
    pre-registered ensembles lost: 84.88, 81.91). Rank averaging is textbook
    (Aggarwal & Sathe 2015; Zimek et al. 2014) — present as evidence for C1 and as a
    demonstration of selection-aware validation, never as a method.
    NOTE: the DeLong test HAS now been run — +1.16 [+0.57, +1.76], p=0.004 (see 1.4 and
    E9), subject to the seed-variance caveat recorded there.

The reproduction remains Experimental Setup, not a numbered contribution.


################################################################################
# 6. WHAT REMAINS
################################################################################
    #  item                                                     cost      why
    1  DONE. Cells 5.4 / 5.5 / 5.7 re-run; both bugs corrected;   --       see 1.1-1.5
       the scalar control and Item 4 have run.
    1b VALIDATE the 5-member set: a3_split_half(members=          seconds  89.46 is the
       A3_MEMBERS_EXT), Cell 5.8b. The 31-subset winner is                 top of 31
       currently selected ON THE TEST SET and is NOT reportable            test-set
       until it survives, exactly as 88.74 was not before 5.2b.            choices
    2  DONE — see E9. C4 is significant.                          --
    3  MULTI-DATASET: VinCXR, BrainTumor, LAG                   ~35 min   the single
       PRE-REGISTER: the post-hoc head should FAIL on           each      highest-value
       BrainTumor, where tumours sit where the AE already                 item; now with
       reconstructs badly. A correctly predicted failure is               a falsifiable
       worth more than a third success.                                   prediction
    4  Ensemble AE-PL with frozen-AE-SSIM+head                  seconds   two scores from
       (a feature-space and a pixel-space reading, from                   opposite spaces;
       DIFFERENT models) — does it beat 88.74?                            the natural test
                                                                          of C1 + C3
    5  Qualitative figure: input / reconstruction / error map   30 min    explains BOTH
       for AE, AE-SSIM, AE-PL                                             the 44.9 cell
                                                                          and M2's -30.56
    6  Two more DAE seeds                                       2.7 h     closes the only
                                                                          reproduction miss
    7  Mixed-objective row in the grid                          ~5 min    closes out the
                                                                          abandoned
                                                                          extension
    8  Two-way analysis with a seed random effect over the grid ~2 h      M3 covers the
                                                                          pairwise gaps;
                                                                          the grid itself
                                                                          still needs it

    Item 4 is new and is the most interesting cheap experiment the results create: C1
    says the two readings live in incompatible spaces, and C4 says scores from different
    models combine well. Combining a feature-space score with a pixel-space score taken
    from a DIFFERENT model is the direct test of both at once, and it costs nothing.

    Total for items 1-5, 7: under 3 hours.
